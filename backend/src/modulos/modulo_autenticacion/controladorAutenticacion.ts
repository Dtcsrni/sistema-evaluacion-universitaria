/**
 * Controlador de autenticacion (placeholder).
 */
import type { Request, Response } from 'express';

export function ingresarDocente(_req: Request, res: Response) {
  res.status(501).json({ mensaje: 'Autenticacion pendiente de implementar' });
}
